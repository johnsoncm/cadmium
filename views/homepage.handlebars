<!-- Loop over each object in the events array - currently showing the projects array from mini-project - need to change to reflect what we want-->
{{#each events as |event| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/event/{{events.id}}">{{events.title}}</a>
    </h2>
    <h3>
      Date: {{format_date events.date}}
      Time: {{events.time}}
      Venue: {{events.locationName}}
      Address: {{events.locationAddress}}
    </h3>
  </div>
  <div class="col-md-7">
    <p>
      {{events.description}}
    </p>
    <p>Special thanks to {{events.user.username}} for adding this event!</p>
  </div>
</div>
{{/each}}
