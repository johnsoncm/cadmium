<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Added by {{user.username}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Add this Event!</button>
  </p>
</div>
